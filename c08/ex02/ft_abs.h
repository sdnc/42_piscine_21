#ifndef FT_ABS_H
# define FT_ABS_H

# define ABS(n) (n < 0 ? -n : n)

#endif
//An absolute value considers how far a number is from zero.
//E.g. 6 is 6 away from zero, and so is -6 (thus, no negatives allowed)
